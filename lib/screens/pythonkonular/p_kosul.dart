import 'package:flutter/material.dart';

class Pkosul  extends StatelessWidget {
  const Pkosul ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}