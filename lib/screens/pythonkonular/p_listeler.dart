import 'package:flutter/material.dart';

class Plisteler extends StatelessWidget {
  const Plisteler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}